.TH PRINTF 3 2025-11-27 GNU "Manuel Utilisateur"

.SH NAME _printf - Recodage de la fonction printf en langage C

.SH SYNOPSIS

#include "main.h"
#include <unistd.h>
#include <stdarg.h>

.BI int _printf(const char *format, ...);

.SH DESCRIPTION Fonction qui affiche une selection dans la sortie standard selon un format defini par l'utilisateur.
La fonction parcourt chacun des caracteres indiques par l'utilisateur, et les affiche s'il s'agit de caracteres standards. Si la fonction detecte un "%d", "%i", "%c", "%s", ou "%%", elle comprend qu'elle doit convertir une variable donnee selon le specificateur trouve. Une fois la conversion effectuee, la fonction conitnue de parcourir la cha

.Sh RETURN VALUE Retourne le nombre de caracteres affiches, ou -1 en cas d'erreur.

.SH FORMAT SPECIFIERS
%c affiche un caractere,
%s affiche une chaine de caracteres,
%% affiche un pourcent ('%'),
%d affiche un entier,
%i affiche un entier

.SH EXAMPLES _printf("Hello %s", world);

.SH AUTHORS
Adele MEGELINK
Laetitia GRONDIN